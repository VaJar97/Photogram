<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vadimvolkov.photogram">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Photogram">
        <activity android:name="com.vadimvolkov.photogram.ProfileActivity"/>
        <activity android:name="com.vadimvolkov.photogram.MainActivity" />
        <activity android:name="com.vadimvolkov.photogram.ShareActivity" />
        <activity android:name="com.vadimvolkov.photogram.LikeActivity" />
        <activity android:name="com.vadimvolkov.photogram.SearchActivity" />

        <activity
            android:name="com.vadimvolkov.photogram.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>